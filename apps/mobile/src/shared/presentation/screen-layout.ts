import { sizes } from "@/shared/presentation/sizes";

export const screenLayout = {
  contentTopGap: sizes.spacing.content,
  headerHeight: 56,
  horizontalPadding: sizes.spacing.content,
} as const;
